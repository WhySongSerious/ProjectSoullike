# ProjectSoullike 작업 규칙

## 프로젝트 기준

- 이 디렉터리가 Unity 프로젝트이자 Git 저장소의 루트다.
- 프로젝트 소유 C# 코드는 `Assets/Soullike/Scripts` 아래에서 관리한다.
- 스크립트는 역할별로 `Combat`, `Player`, `Enemies/<EnemyName>`, `Editor/Setup`처럼 나눈다. 모든 코드를 한 폴더에 평평하게 모으지 않는다.
- Unity에서 바로 사용하는 외부 에셋은 `Assets/ThirdParty/<AssetName>` 아래에 묶는다. 벤더가 제공한 내부 구조와 파일명은 특별한 이유가 없으면 유지한다.
- 원본 패키지, 압축 파일, 작업 원본처럼 Unity가 직접 임포트할 필요가 없는 자료는 저장소 밖 `../SourceAssets`에 둔다.
- `Assets/Soullike/Gun`은 출처가 확정되기 전까지 임의로 프로젝트 소유 또는 외부 에셋으로 재분류하지 않는다.

## Unity 파일 관리

- Unity 에셋을 이동하거나 이름을 바꿀 때 대응하는 `.meta` 파일도 함께 처리해 GUID를 보존한다.
- `Library`, `Temp`, `Logs`, `UserSettings`와 빌드 산출물은 커밋하지 않는다. 기존 `.gitignore`를 따른다.
- 씬, 프리팹, 머티리얼처럼 참조가 중요한 파일을 변경한 뒤에는 누락된 참조와 Unity 콘솔 오류를 확인한다.
- 사용자가 Unity 에디터에서 만든 변경과 현재 작업 트리의 미완성 변경을 임의로 되돌리거나 덮어쓰지 않는다.

## 작업 방식

- 먼저 관련 파일과 현재 Git 상태만 좁게 확인하고, 한 번에 하나의 논리적인 변경 단위로 작업한다.
- 검색은 `rg`와 `rg --files`를 우선 사용한다. `Library` 같은 생성 폴더 전체를 읽거나 불필요하게 대량 출력하지 않는다.
- 일상적인 세부사항은 합리적으로 판단해 진행하되, 결과가 크게 달라지는 선택이나 파괴적 작업만 사용자에게 확인한다.
- 기존 구조와 명명 규칙을 우선하며, 새 추상화나 폴더는 실제로 반복되거나 경계가 필요한 경우에만 추가한다.

## 검증

- 런타임 C# 변경 후에는 최소한 `dotnet build Assembly-CSharp.csproj`를 실행한다.
- 에디터 C# 변경 후에는 `dotnet build Assembly-CSharp-Editor.csproj`도 실행한다.
- 동작, 씬 또는 에셋 참조가 바뀌면 가능한 범위에서 Unity 콘솔, 플레이 모드 또는 관련 씬을 추가 확인한다.
- 문서만 변경한 경우 빌드는 생략할 수 있지만, `git diff --check`로 형식 오류를 확인한다.

## Git 운영

- 사용자는 이 프로젝트에서 검증된 논리적 체크포인트가 생기면 별도 확인 없이 커밋하고 원격 저장소에 푸시하는 것을 허용했다.
- 커밋 전 diff와 상태를 확인하고, 이번 작업과 무관하거나 미완성인 사용자 변경은 스테이징하지 않는다.
- 검증되지 않은 변경, 비밀 정보, 생성 파일은 커밋하거나 푸시하지 않는다.
- 현재 브랜치를 기본으로 사용하고, 요청 없이 브랜치를 바꾸거나 파괴적인 Git 명령을 실행하지 않는다.
- 커밋 메시지는 변경 목적이 드러나게 작성한다. 커밋 또는 푸시했다면 최종 보고에 커밋 해시와 결과를 포함한다.

## 소통

- 기본 응답 언어는 한국어로 한다.
- 진행 중에는 짧게 범위와 중요한 판단을 알리고, 완료 시 변경점과 검증 결과를 간결하게 정리한다.
